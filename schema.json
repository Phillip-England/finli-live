{
  "variables": [
    {
      "name": "DB_PATH",
      "description": "Path to the primary SQLite database used for the public invoice-generation IP usage ledger.",
      "example": "data/main.sqlite",
      "required": false
    },
    {
      "name": "PORT",
      "description": "HTTP port the application listens on.",
      "example": "9876",
      "required": false
    },
    {
      "name": "TRUST_PROXY",
      "description": "Whether to trust X-Forwarded-For and X-Real-IP headers from a known reverse proxy.",
      "example": "false",
      "required": false
    }
  ]
}
